export default {
  // Add custom theme definitions here
  // Each theme should have a 'default' style, after that it's up to you!
  // Styles can be an array of class strings or a single string of space-delimited class names

  /* this is an example of a "traditional" css theme with bootstrap-y style names
  light: {
    default: ['bg-dark', 'text-white'], // can be array
    primary: 'bg-primary text-white',   // or space-delimited string
    secondary: 'bg-secondary text-white',
    success: 'bg-success text-white',
    danger: 'bg-danger text-white',
    warning: 'bg-warning text-dark',
    info: 'bg-info text-white'
  },
  */

  /* this is an example of a "utility" css theme with tailwind classes and more color-focused style names
  dark: {
    default: ['transition', 'duration-500', 'ease-in-out', 'bg-gray-900', 'text-white'],
    blue: 'transition duration-500 ease-in-out bg-blue-600 text-white',
    green: 'transition duration-500 ease-in-out bg-green-600 text-white',
    red: 'transition duration-500 ease-in-out bg-red-600 text-white',
    orange: 'transition duration-500 ease-in-out bg-orange-600 text-white',
  }
};
