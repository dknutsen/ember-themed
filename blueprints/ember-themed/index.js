'use strict';

module.exports = {
  description: 'Sets up ember-themed with a service and themes file',

  normalizeEntityName: function() {
    // this prevents an error when the entityName is
    // not specified (since that doesn't actually matter
    // to us
  }
};
